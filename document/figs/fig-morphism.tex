\documentclass[tikz,margin=1mm]{standalone}


\begin{document}
\begin{tikzpicture}[node distance=2cm, auto]
  \node (LE) {$\mathcal{L}_E$};
  \node[above of=LE, right of=LE] (EG) {$E_G$};
  \node[right of=EG] (VG) {$V_G$};
  \node[below of=LE, right of=LE] (EH) {$E_H$};
  \node[right of=VG, below of=VG] (LV) {$\mathcal{L}_V$};
  \node[below of=LV, left of=LV] (VH) {$V_H$};

  \draw[->] (EG) to node{$s_G$/$t_G$} (VG);
  \draw[->] (EG) to node[swap]{$m_G$} (LE);
  \draw[->] (EG) to node[swap]{$g_E$} (EH);
  \draw[->] (EH) to node[swap]{$s_H$/$t_H$} (VH);
  \draw[->] (VG) to node{$l_G$} (LV);
  \draw[->] (VG) to node{$g_V$} (VH);
  \draw[->] (VH) to node[swap]{$l_H$} (LV);
  \draw[->] (EH) to node{$m_H$} (LE);
\end{tikzpicture}
\end{document}